背景： 我用的STM32+LWIP做远程升级。文件服务器会传一个BIN文件,需要用MD5进行校验完整性。我先将网络传来的文件存储在外部的flash W25Q64中(传输采用类似Ymode方式 每次也是传的2K,每次2k 2K的写入W25Q64)
。再从Flash中读出bin文件 由于我的STM32芯片内存很小故需要分片读取校验，每次读2k 然后进行校验,最后进行拼接。得到整个文件的MD5值 经过测试可以正确使用。我的Bin大小是164K



